// Re-export from context for backwards compatibility
// All components now share the same state through the context
export { useUserLimits, clearUserLimitsCache } from '@/contexts/UserLimitsContext'
